function skillsMember() function skillsMember() {
  const member = this;
  const skills = member.skills;
  const skill = skills[0];
  const skillName = skill.name;
  const skillLevel = skill.level;
  const skillYears = skill.years;
  const skillDescription = skill.description;
  const skillLink = skill.link;
  const skillImage = skill.image;
  const skillImageAlt = skill.imageAlt;
  const skillImageTitle = skill.imageTitle;
  const skillImageWidth = skill.imageWidth;
  const skillImageHeight = skill.imageHeight;
  const skillImageCaption = skill.imageCaption;
  const skillImageCaptionLink = skill.imageCaptionLink;
  const skillImageCaptionLinkText = skill.imageCaptionLinkText;
  const skillImageCaptionLinkTitle = skill.imageCaptionLinkTitle;
  const skillImageCaptionLinkTarget = skill.imageCaptionLinkTarget;
  const skillImageCaptionLinkRel = skill.imageCaptionLinkRel;
  const skillImageCaptionLinkRev = skill.imageCaptionLinkRev;
  const skillImageCaptionLinkLang = skill.imageCaptionLinkLang;
  const skillImageCaptionLinkType = skill.imageCaptionLinkType;
  const skillImageCaptionLinkMedia = skill.imageCaptionLinkMedia;
  const skillImageCaptionLinkHreflang = skill.imageCaptionLinkHreflang;
  const skillImageCaptionLinkDownload = skill.imageCaptionLinkDownload;
  const skillImageCaptionLinkPing = skill.imageCaptionLinkPing;
  const skillImageCaptionLinkReferrerpolicy = skill.imageCaptionLinkReferrerpolicy;
  const skillImageCaptionLinkTargetBlank = skill.imageCaptionLinkTargetBlank;
  const skillImageCaptionLinkTargetParent = skill.imageCaptionLinkTargetParent;
  const skillImageCaptionLinkTargetSelf = skill.imageCaptionLinkTargetSelf;
  const skillImageCaptionLinkTargetTop = skill.imageCaptionLinkTargetTop;
  const skillImageCaptionLinkTargetFrame = skill.imageCaptionLinkTargetFrame;
  const skillImageCaptionLinkTargetIframe = skill.imageCaptionLinkTargetIframe;
  const skillImageCaptionLinkTargetPopup = skill.imageCaptionLinkTargetPopup;
  const skillImageCaptionLinkTargetMail = skill.imageCaptionLinkTargetMail;
  const skillImageCaptionLinkTargetCustom = skill.imageCaptionLinkTargetCustom;
  const skillImageCaptionLinkTargetCustomName = skill.imageCaptionLinkTargetCustomName;
  const skillImageCaptionLinkTargetCustomFeatures = skill.imageCaptionLinkTargetCustomFeatures;
  const skillImageCaptionLinkTargetCustomFeatures